#include<stdio.h>
int main(void)
{
	int s,i,count=0,n;
	printf("enter the value of s \t");
	scanf("%d",&s);
	printf("enter the value of n");
	scanf("%d",&n);
	if(count==2)
	printf("%d is the prime number",s);
	else
	printf("%d is not a prime number",s);
	return 0;
	
}
